export interface PlanetModelCreate {
  id: string;
  name: string;
  climate: string;
  ground: string;
}

export interface PlanetModel {
  id: string;
  name: string;
  climate: string;
  ground: string;
  movies: number;
}
